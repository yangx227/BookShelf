# 个人书架管理网页应用 - 产品需求文档 (PRD)

**版本**: 1.1  
**日期**: 2025-03-14  
**目标**: 定义个人书架应用的最小可行产品 (MVP) 需求与技术方案。**无后端，数据仅存于浏览器本地。**

---

## 1. 项目概述

### 1.1 产品定位

个人书架管理网页应用，用于用户管理个人藏书、阅读状态、评分与笔记，支持按书名/作者搜索与按阅读状态筛选。数据仅保存在当前浏览器本地，无需服务器。

### 1.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | React 18+ | UI 框架，配合 TypeScript |
| 前端构建 | Vite | 开发与构建 |
| 数据存储 | 浏览器 localStorage | 仅前端持久化，刷新/下次打开仍保留 |

**说明**：本项目不包含后端服务，无需 Node.js 服务器。单机运行 `npm run dev` 即可使用。

---

## 2. 用户角色与场景

- **角色**: 个人用户，单机本地使用。
- **场景**: 添加/编辑书籍、标记阅读状态、打分、写笔记、按条件搜索与筛选；数据保存在本机浏览器中。

---

## 3. 功能需求 (MVP)

### 3.1 书籍卡片展示

| 需求项 | 描述 | 优先级 |
|--------|------|--------|
| 书籍列表 | 以卡片形式展示所有书籍，每张卡片包含：封面图、书名、作者、阅读状态、评分 | P0 |
| 卡片布局 | 响应式网格（如 2/3/4 列），支持移动端 | P0 |
| 空状态 | 无书籍时展示引导文案与「添加书籍」入口 | P1 |
| 初始数据 | 首次打开时若无数据，可预置若干本示例书籍 | P1 |

**书籍数据模型**:

```ts
interface Book {
  id: string;           // UUID
  title: string;        // 书名
  author: string;       // 作者
  coverUrl?: string;    // 封面图 URL（可选）
  status: 'reading' | 'read' | 'want';  // 在读、已读、想读
  rating?: number;      // 1-5 星评分，可选
  notes?: string;       // 笔记内容，可选
  createdAt: string;    // ISO 8601
  updatedAt: string;
}
```

### 3.2 搜索框（按书名与作者过滤）

| 需求项 | 描述 | 优先级 |
|--------|------|--------|
| 搜索输入 | 单输入框，占位符提示「按书名或作者搜索」 | P0 |
| 过滤逻辑 | 实时或防抖（如 300ms）过滤，匹配书名或作者（大小写不敏感、部分匹配） | P0 |
| 清空 | 支持一键清空搜索内容 | P1 |

### 3.3 筛选标签页（全部 / 在读 / 已读 / 想读）

| 需求项 | 描述 | 优先级 |
|--------|------|--------|
| 标签项 | 四个选项：全部、在读、已读、想读 | P0 |
| 交互 | 点击切换，当前选中高亮；与搜索条件共同作用（先按状态筛选，再按关键词搜索） | P0 |
| 可选计数 | 每个标签旁展示对应数量（如「在读 (3)」） | P1 |

**状态枚举**: `all` | `reading` | `read` | `want`

### 3.4 书籍评分与笔记

| 需求项 | 描述 | 优先级 |
|--------|------|--------|
| 评分 | 每本书支持 1–5 星评分，可修改与清空 | P0 |
| 笔记 | 每本书支持多行文本笔记，可编辑与清空 | P0 |
| 展示位置 | 卡片上展示评分；详情弹窗中展示/编辑完整笔记 | P0 |
| 持久化 | 评分与笔记随书籍一起写入 localStorage，刷新后仍存在 | P0 |

---

## 4. 页面与组件结构

### 4.1 页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 书籍列表页 | `/` | 主页面：搜索框 + 筛选标签 + 书籍卡片列表 |

书籍详情与编辑在弹窗中完成，无独立路由。

### 4.2 React 项目代码结构

```
BookShelf/
├── prd.md
├── package.json
├── vite.config.ts
├── index.html
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── index.css
│   ├── vite-env.d.ts
│   │
│   ├── lib/
│   │   └── booksStorage.ts   # 本地数据层：localStorage 读写与初始数据
│   │
│   ├── components/
│   │   ├── BookCard.tsx
│   │   ├── SearchBox.tsx
│   │   ├── FilterTabs.tsx
│   │   ├── BookDetailModal.tsx
│   │   ├── AddBookModal.tsx
│   │   └── ErrorBoundary.tsx
│   │
│   ├── pages/
│   │   └── BookListPage.tsx
│   │
│   ├── types/
│   │   └── book.ts
│   │
│   └── hooks/
│       └── useBooks.ts       # 书籍状态与列表，基于 lib/booksStorage
```

无 `server/` 或后端目录要求；应用仅前端单页 + localStorage。

### 4.3 组件职责简述

- **BookListPage**: 组合搜索、筛选、列表；管理筛选状态与书籍数据（通过 useBooks）。
- **SearchBox**: 受控输入，防抖后用于按书名/作者过滤。
- **FilterTabs**: 全部/在读/已读/想读切换，与搜索共同筛选。
- **BookCard**: 单本书展示，点击打开详情弹窗。
- **BookDetailModal**: 展示/编辑阅读状态、评分、笔记，保存时写入 localStorage。
- **AddBookModal**: 添加新书，保存时写入 localStorage。

---

## 5. 本地数据层（无后端 API）

### 5.1 约定

- **存储位置**: 浏览器 `localStorage`，键名固定（如 `bookshelf-books`）。
- **数据格式**: JSON 数组，元素为 `Book`。
- **ID**: 使用 UUID 或足够唯一的字符串。

### 5.2 数据操作（前端实现）

在 `lib/booksStorage.ts`（或等价模块）中实现与以下语义一致的方法，供 `useBooks` 等调用：

| 操作 | 说明 |
|------|------|
| 读取列表 | 从 localStorage 读取全部书籍；支持按 `status`、关键词 `q` 在内存中过滤后返回。 |
| 读取单本 | 根据 id 在本地数组中查找并返回。 |
| 创建 | 生成 id、时间戳，追加到数组并写回 localStorage。 |
| 更新 | 根据 id 更新对应字段（含评分、笔记），更新 `updatedAt`，写回 localStorage。 |
| 删除 | 从数组中移除对应 id，写回 localStorage。 |

**首次使用**：若 localStorage 中无数据，可写入一份预置书单（如 10 本示例书），再供用户增删改。

### 5.3 过滤逻辑

- **status**: `all` 表示不过滤；`reading` / `read` / `want` 只保留对应状态。
- **q**: 对书名、作者做大小写不敏感、部分匹配（子串或分词由实现决定）。

无需分页时，可在前端一次取全量再过滤；数据量增大后再考虑分页或虚拟列表。

---

## 6. 技术建议

### 6.1 前端

- **状态**: 列表、筛选/搜索条件用 React `useState`；数据源为 localStorage 封装（lib），通过 `useBooks` 提供列表与增删改。
- **数据层**: 在 `lib/booksStorage.ts` 中封装读写；`useBooks` 调用该层并维护当前列表与 loading/error（若需要）。
- **搜索/筛选**: 前端从 localStorage 取全量，在内存中按 status + q 过滤；防抖 300ms 适用于搜索输入。
- **样式**: 保持现有 CSS/变量体系即可。

### 6.2 运行方式

- 仅需启动前端：在项目根目录执行 `npm run dev`，在浏览器打开提示的本地地址（如 http://localhost:5173）。无需启动任何后端服务。

---

## 7. 非功能需求（MVP 可简化）

- **性能**: 列表首次加载即时；搜索/筛选防抖后即时响应。
- **兼容**: 支持现代浏览器；依赖 localStorage 的浏览器即可使用。
- **无障碍**: 表单与按钮具备可读的 label 与焦点顺序（后续可加强）。

---

## 8. 验收标准（MVP）

1. 书籍列表以卡片形式展示，包含书名、作者、状态、评分。
2. 搜索框输入后，列表仅显示书名或作者包含关键词的书籍。
3. 点击「全部 / 在读 / 已读 / 想读」可正确筛选列表。
4. 可对单本书进行 1–5 星评分并保存。
5. 可对单本书编辑笔记并保存。
6. 以上数据保存在浏览器 localStorage，刷新页面或下次打开仍存在；无需后端。

---

## 9. 后续迭代方向（非 MVP）

- 数据导出/导入（JSON 文件）、暗色主题、PWA 离线支持。
- 若将来需要多设备同步，可再引入后端或云存储，届时再定义接口与迁移方案。

---

**文档结束**。实现以本 PRD 为准，数据模型与本地数据层保持一致，无后端依赖。
